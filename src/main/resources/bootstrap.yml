spring:
  application:
    name: carinspection

  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      # optionally, profile and label if needed
      # profile: default
      # label: main
